using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fail : BaseState
{
    public Fail(string _stateName,StateMachine _stateMachine) : base(_stateName, _stateMachine)
    {
    }
}
